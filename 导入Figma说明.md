# Hazel Li（李瑭）作品集：Figma 与动态网站的区别

`figma-sections` 中的 8 张图片是较早版本的静态设计记录，不是这次更新后的动态网站。它们可以用来回看版式方向，但不应再作为当前成品。

## 当前版本应该在哪里查看

请把完整项目导入 StackBlitz，或在电脑中运行 `pnpm dev`。只有浏览器预览能完整展示：

- 动态背景与滚动动效
- TikTok 与 Bilibili 播放器
- 点击后扩展的电影播放器
- 项目详情、图片放大与键盘返回
- 手机、平板与桌面响应式布局

Figma 可以继续承载静态画板和评论，但无法等价保存 React 网站的真实播放器与网页动效。需要修改内容时，优先编辑 `src/data.ts`；需要修改视觉时，编辑 `src/styles.css`。详细步骤见 `StackBlitz导入与编辑说明.md`。
